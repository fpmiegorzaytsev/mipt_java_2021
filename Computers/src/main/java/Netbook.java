import java.security.Key;

public class Netbook extends PortableComputer{
    public Netbook(String maker, String model, String processor, String RAM, String HDD, Display display, Keyboard keyboard){
        super(maker, model, processor, RAM, HDD, display, keyboard);

    }

}
